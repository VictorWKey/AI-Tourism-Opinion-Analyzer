# 1. Instala spaCy específicamente primero
conda activate ultima
pip install "spacy>=3.7.0,<3.9.0"

# 2. Instala las dependencias problemáticas una por una
pip install bertopic
pip install umap-learn
pip install hdbscan
pip install nltk
pip install langdetect

# 3. Instala el resto
pip install -r requirements.txt --no-deps

conda activate ultima && pip install gensim

conda activate ultima && pip install torchvision